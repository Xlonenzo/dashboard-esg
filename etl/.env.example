# =============================================================================
# CONFIGURACOES DO ETL - ESG BANCO VOTORANTIM
# Copie este arquivo para .env e ajuste conforme necessario
# =============================================================================

# MODO DE CONEXAO: "local" ou "azure"
DB_MODE=local

# =============================================================================
# CONFIGURACAO LOCAL (SQL Server instalado na maquina)
# =============================================================================
# Opcoes de servidor:
#   localhost              -> SQL Server padrao
#   localhost\SQLEXPRESS   -> SQL Server Express
#   (localdb)\MSSQLLocalDB -> LocalDB (Visual Studio)
#   .\SQLEXPRESS           -> SQL Server Express (alternativo)

LOCAL_SQL_SERVER=localhost
LOCAL_SQL_DATABASE=ESG_BV

# Deixe vazio para usar Windows Authentication (recomendado para local)
LOCAL_SQL_USER=
LOCAL_SQL_PASSWORD=

# OU use SQL Authentication:
# LOCAL_SQL_USER=sa
# LOCAL_SQL_PASSWORD=sua_senha

# =============================================================================
# CONFIGURACAO AZURE (quando DB_MODE=azure)
# =============================================================================
AZURE_SQL_SERVER=seu-servidor.database.windows.net
AZURE_SQL_DATABASE=ESG_BV
AZURE_SQL_USER=seu_usuario
AZURE_SQL_PASSWORD=sua_senha
